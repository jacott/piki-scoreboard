d* Render org list

d* Render user list

d* Add user account and set password to "changeme"

d* Validate user

d* Add change user password page

* Add forgotten password page

* Add/remove competitor to startlist when add/changing categories

 - aftersave change EventCat with uniqiue random sortKey











* Example to change content on focused input box

  // 'webkitspeechchange input[name=speech], speechchange input[name=speech]': function (event,templ) {
  //   var ta = templ.find('textarea'),
  //       speech = event.target.value;
  //   if ('selectionStart' in ta) {
  //     var startPos = ta.selectionStart,
  //         endPos = ta.selectionEnd,
  //         space = ta.value.substring(endPos, endPos+1) === ' ' ? '': ' ';
  //     ta.value = ta.value.substring(0, startPos)
  //       + speech + space + ta.value.substring(endPos);
  //     ta.selectionStart = ta.selectionEnd = startPos + speech.length+1;
  //   } else {
  //     ta.value = ta.value + event.target.value + ' ';
  //   }

  //   ta.focus();
  // },
