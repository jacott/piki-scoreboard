#!/bin/bash
set -em

cd `dirname "$0"`/../app

# export NODE_OPTIONS='--prof'
# send SIGUSR1 to debug

type=${1:-demo}
shift

if [ $type = demo ];then
    type="."
fi
exec node ${type}/server.js "$@"
